import React from 'react'
import styles from '../../index.module.less'

const OptionsArea = (): JSX.Element => {
	return <div className={styles.options}>this is page</div>
}

export default OptionsArea
